server:
  port: ${PORT:5000}

name: Service-BusinessCoreService

eureka:
  instance.metadataMap:
    appType: core-service

spring:
  cloud:
    stream:
      bindings:
        input: 
          destination: profiles
          binder: kafka